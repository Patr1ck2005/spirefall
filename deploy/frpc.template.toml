# frpc client config for Spirefall — runs on the HOST's PC (Windows).
# Generated by deploy\start-frp.ps1; regenerate there, do not hand-edit the token.
serverAddr = "VPS_IP_PLACEHOLDER"
serverPort = 7000
auth.token = "TOKEN_PLACEHOLDER"

[[proxies]]
name = "spirefall-web"
type = "tcp"
localIP = "127.0.0.1"
localPort = 8787
remotePort = 7100
